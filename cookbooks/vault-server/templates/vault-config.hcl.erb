listener "tcp" {
  address = "0.0.0.0:8200"
  tls_cert_file="<%= @certpath %>/server.crt"
  tls_key_file="<%= @certpath %>/server.key"
}

backend "s3" {
  bucket = "615197564783-vault"
  region = "us-east-1"
}

disable_mlock=true
